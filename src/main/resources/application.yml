spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL94Dialect
    hibernate:
        ddl-auto: validate
  datasource:
    url: jdbc:postgresql://localhost:8888/charity
    username: root
    password: qaz

server:
  port: 8080
